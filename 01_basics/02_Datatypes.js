"use strict"; // treat all js code as newer version.

// alert(3+3) // we are using nodejs, not brwoser so it wil throw error in console.

console.log(3 +
    3
); // code readability should be high.

console.log("Nouman");


// Datatypes:- Primitive datatypes & Non-Primitive

// Number -> ranges from 2 to power 53, this can a single memory can allocate a value for a number specfic varaible.
// bigInt
// string -> ""
// boolean -> true/false
// null -> It's standalone value
// undifined ->
// symbol -> unique, in terms of reactjs component perspective.

// Below are the examples of obove mentioned datatypes,

let age = 25;
let name = "nouman"
let isLoggedIn = false;
let state;

// object

console.log(typeof undefined); // undefined
console.log(typeof null);  // object

