const gitHubName = "03nouman";
let repoCount = 70;

console.log(`The GitHub name is ${gitHubName} and my repo count is ${repoCount}`);// this type of strings are called modern strings format

const discordName = new String('nouman1234')